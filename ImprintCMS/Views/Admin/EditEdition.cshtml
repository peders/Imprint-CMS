@model Edition
@{
	ViewBag.Title = Phrases.PagenameEditEdition;
	ViewBag.Breadcrumbs = new[] {
		Html.ActionLink(Phrases.PagenameBooks, "books"),
		Html.ActionLink(Model.Book.FullTitle, "editbook", new { id = Model.BookId }),
		Phrases.PagenameEditEdition.ToHtmlString()
	};
}
@Html.EditorForModel()
