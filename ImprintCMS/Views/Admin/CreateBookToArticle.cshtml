@model BookToArticle
@{
	ViewBag.Title = Phrases.PagenameCreateBookLink;
	ViewBag.Breadcrumbs = new[] {
		Html.ActionLink(Phrases.PagenameArticles, "articles"),
		Html.ActionLink(Model.Article.Title, "editarticle", new { id = Model.ArticleId }),
		Phrases.PagenameCreateBookLink.ToHtmlString()
	};
}
@using (Html.BeginForm())
{
	@Html.ValidationSummary(true)
	<fieldset class="fields">
		<p>
			<label>@Phrases.LabelArticle</label><br />
			@Model.Article.Title
		</p>
		<p>
			@Html.LabelFor(model => model.BookId, Phrases.LabelBook)<br />
			@Html.DropDownListFor(model => model.BookId, (SelectList)ViewBag.Books, "–")
			@Html.ValidationMessageFor(model => model.BookId)
		</p>
	</fieldset>
	<fieldset class="hidden">
		@Html.HiddenFor(model => model.ArticleId)
		@Html.HiddenFor(model => model.SequenceIdentifier)
	</fieldset>
	<fieldset class="submit">
		<p>
			<input type="submit" value="@Phrases.LabelSave" />
		</p>
	</fieldset>
}
